function Reverse() {
    var array = [1,3,4,3,2,2];
    const len = aarray.length;
    var revArr = new Array1;
    var k = 0;
    for (var i = len - 1; i >= 0; i--) {
        revArr.push(a[i]);
        console.log(revArr[k]);
        //document.write(revArr[k] + " ");
        k++;

    }
}